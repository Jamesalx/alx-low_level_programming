#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * main - a function that checks for uppercase character
 *Returns 1 if c is uppercase
 *Returns 0 otherwise
 *
 */

int _isupper(int c)
{
	int ch = 'c';

	if (isupper(ch))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
