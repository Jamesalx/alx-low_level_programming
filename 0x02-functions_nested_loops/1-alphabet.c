#include <unistd.h>

/**
 * main-function that prints the alphabet, in lowercase.
 *
 * Return: (0) Success
 */

void print_alphabet(void);

int main(void);
{
	(ch = 'a');

	for (ch = 'a'; <= 'z'; ch++);
	putchar(ch);
	putchar('\n');

	return (0);
}
