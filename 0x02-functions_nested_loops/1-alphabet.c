#include "main.h"

/**
 * main-function that prints the alphabet, in lowercase.
 *
 *
 */

void print_alphabet(void);
{
	(ch = 'a');

	for (ch = 'a'; <= 'z'; ch++);

	_putchar(ch);
	_putchar('\n');
}
