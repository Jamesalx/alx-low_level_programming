#include "main.h"

/**
 * main- function that computes the absolute value of an integer.
 *
 *  Return: Always 0.
 */

int _abs(int)
{
	if (i >= 0)
	{
	return (i);
	}
	else
	{
		return (i * -1);
	}
	return (0);
}


