#include "main.h"

/**
 * main - a function that prints 10 times the alphabet, in lowercase.
 *
 * Return: Always 0.
 */

void print_alphabet_x10(void);

int main(void)
{
	char ch = 'a';
	
	for (ch = 'a'; ch <= 'z'; ch+10);
	int putchar(ch);
	int putchar('\n');

	return (0);
}
