#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"
/**
 * main-function that returns the number of elements in a linked list
 * struct list_s - singly linked list
 * @str: string - (malloc'ed string)
 * @len: length of the string
 * @next: points to the next node
 * Return: number of elements
 */
size_t list_len(const list_t *h)
typedef struct list_s
{
    char *str;
    unsigned int len;
    struct list_s *next;
} list_t;

	typedef struct *h = head
	while(h != NULL)
{
	 printf("%d =>", h -> str);
        h = h -> next;
	{
	for(len = 0; len <= list_t; len++)
		printf("%d\n", len);
	}
	return (len);
}
	
