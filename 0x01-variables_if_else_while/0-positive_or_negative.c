#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main(void)
{
	int n;
	if 

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	printf("%d is a positive number,n\n");
	printf("%d is a negative number,n\n");
	printf("%d\n", rand_num);
	return (0);
}
