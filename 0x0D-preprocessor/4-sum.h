#ifndef SUM(x, y)
#define SUM(x, y) (x + y)
/**
 *main - computes the sum of the numbers x and y
 *
 *Return: (0);
 */
int main(void)
{
	int num1, num2;
	printf("sum(%d, %d) = %d\n", x, y, SUM(x, y));
	return (0);
}
#endif
