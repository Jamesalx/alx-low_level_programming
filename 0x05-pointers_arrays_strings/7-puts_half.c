#include "main.h"
#include <stdio.h>

/**
 * main - function that prints half of a string, followed by a new line. 
 *
 * Return: Always 0.
 */

void puts_half(char *str)
{
	char str;

	str = "Hello world";

}
