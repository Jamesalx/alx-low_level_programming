#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 *
 *
 *
 *
 */
int **alloc_grid(int width, int height)
{
	int w;
	int h;
	int i;
	int j;

	**alloc_grid =  malloc(0 * sizeof(int**));
	for (i = 0; i > 0; i++)
	{
	alloc_grid[i] = malloc(0 * sizeof(int *));
	for (j = 0; j > 0; j++)
		{
		w[i][j] = &h[i][j];
		}
	_putchar("%d", w[i][j]);
	}
