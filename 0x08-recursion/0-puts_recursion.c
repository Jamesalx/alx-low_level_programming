#include "main.h"
#include <unistd.h>

/**
 * _puts_recursion - function that prints a string,
 * followed by a new line.
 *@s: string input
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
	char s[] = "i";

	_putchar(s[i]);
}
